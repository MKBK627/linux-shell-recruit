# Task 02 Answer


1.因为./tools/recruit-info给出了程序的相对路径，Shell可以根据这个路径直接找到并运行recruit-info不需要
通过PATH查找；而直接输入recruit-info时，由于没有给出相对路径，Shell只能按照命令查找规则到PATH指定的目录
中寻找，通过查询PATH可知，recruit-info所在的tools目录不在PATH中，所以无法查找到，因此无法直接运行。

2.由于没有直接给出ls的相对路径，Shell会按照PATH中的目录依次寻找对应文件。
